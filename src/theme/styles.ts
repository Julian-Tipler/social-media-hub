
import {colors} from "./colors"

export const styles = {
  global: {
    // styles for the `body`
    body: {
      bg: colors.brand.background,
      color: "#fff"
    },
    // styles for the `a`
    a: {
      _hover: {
        textDecoration: "underline",
      },
    },
  },
};
